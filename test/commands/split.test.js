const { expect, test } = require("@oclif/test");

// Need to write a proper test
// describe("split", () => {
//   test
//     .stdout()
//     .command(["split"])
//     .it("runs split", (ctx) => {
//       expect(ctx.stdout).to.contain("split world");
//     });
// });
